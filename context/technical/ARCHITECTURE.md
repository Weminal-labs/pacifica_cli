# Architecture

> Generated by setup wizard on 2026-03-29. Updated 2026-03-30 to reflect implemented structure.

## System Overview

```
┌─────────────────────────────────────────────────────────────┐
│                     User / AI Agent                          │
├─────────────┬──────────────────┬────────────────────────────┤
│  CLI (TUI)  │   MCP Server     │   Claude Code Skills       │
│  Ink + cmdr │   MCP SDK        │   Slash commands           │
├─────────────┴──────────────────┴────────────────────────────┤
│                    Core Library                              │
│  ┌──────────────┬──────────────┬──────────────┐             │
│  │ SDK Client   │ Smart Orders │ Risk         │             │
│  │ REST + WS    │ Manager      │ Calculator   │             │
│  ├──────────────┼──────────────┼──────────────┤             │
│  │ Config       │ Guardrails   │ Journal      │             │
│  │ .pacifica.yml│ Enforcement  │ Logger       │             │
│  └──────────────┴──────────────┴──────────────┘             │
├─────────────────────────────────────────────────────────────┤
│                  External APIs                               │
│  ┌──────────────────────────────────────────────┐           │
│  │ Pacifica REST + WebSocket                     │           │
│  └──────────────────────────────────────────────┘           │
└─────────────────────────────────────────────────────────────┘
```

## Directory Structure

```
src/
├── cli/                    # CLI entry point and commands
│   ├── index.ts           # Commander setup, command registration
│   ├── theme.ts           # Terminal styling and formatting helpers
│   ├── commands/          # One file per command group
│   │   ├── scan.tsx       # pacifica scan (Ink component)
│   │   ├── trade.ts       # pacifica trade buy/sell
│   │   ├── orders.ts      # pacifica orders / cancel
│   │   ├── positions.ts   # pacifica positions / close
│   │   ├── heatmap.ts     # pacifica heatmap (Ink component)
│   │   ├── funding.ts     # pacifica funding
│   │   ├── agent.ts       # pacifica agent status/stop/start/config/log
│   │   ├── smart.ts       # pacifica smart trailing/list/cancel
│   │   ├── journal.ts     # pacifica journal
│   │   └── init.ts        # pacifica init (onboarding wizard)
│   └── components/        # Shared Ink components
│       └── MarketTable.tsx # Live market data table
├── core/                   # Shared business logic
│   ├── sdk/               # Pacifica API wrapper
│   │   ├── client.ts      # REST client with Ed25519 auth
│   │   ├── signer.ts      # Ed25519 request signing
│   │   ├── websocket.ts   # WebSocket client with reconnection
│   │   └── types.ts       # API response types
│   ├── config/            # Config file management
│   │   ├── loader.ts      # Load/parse .pacifica.yaml
│   │   └── types.ts       # Config TypeScript types
│   ├── agent/             # Agent safety modules
│   │   ├── guardrails.ts  # Check action against limits
│   │   ├── spending-tracker.ts # Daily spending tracker
│   │   └── action-logger.ts   # Append-only audit trail
│   ├── journal/           # Trade journal logger
│   │   └── logger.ts      # Append to journal.json
│   ├── smart/             # Smart order manager
│   │   ├── manager.ts     # Poll loop, order lifecycle
│   │   └── types.ts       # Smart order types
│   └── risk/              # Risk analysis
│       └── calculator.ts  # Position risk calculations
└── mcp/                   # MCP server
    └── server.ts          # All 23 MCP tools (10 read + 5 analytics + 2 funding + 6 write)
```

## Data Flow

### Trading Flow (Human via CLI)
1. User runs `pacifica trade buy ETH-PERP 0.5 --lev 5 --tp 4000 --sl 3700`
2. Commander parses args → validates inputs
3. Core SDK sends REST request to Pacifica API
4. Response: order confirmation with fill price, fees
5. CLI prints formatted confirmation

### Trading Flow (Agent via MCP)
1. Agent calls `pacifica_place_order` MCP tool
2. Guardrails checker: Is agent enabled? Is action allowed? Within spending limit? Under confirmation threshold?
3. If guardrails pass → Core SDK sends REST request
4. Response returned as structured JSON to agent
5. Agent log appends action to `.pacifica/agent-log.json`
6. Spending tracker records the trade

### Live Data Flow
1. WebSocket connects to Pacifica on startup
2. Ticker updates stream in → update React state in Ink components
3. `pacifica scan` re-renders table on every tick
4. If disconnect > 5s → fallback to REST polling every 2s
5. On reconnect → resume WebSocket, stop polling

## Key Design Decisions

- See context/project/DECISIONS.md for full decision log
- Core library is shared between CLI, MCP server, and Skills — no duplication
- Guardrails enforcement happens in core, not at the CLI or MCP layer
- All write operations go through the same path regardless of trigger source
